enum UserStatus {
    ONLINE
    OFFLINE
}

enum UserRole {
    SUPER_ADMIN
    ADMIN
    WAREHOUSE_STAFF // should read Warehouse Staff
    PURCHASING_AGENT
    MEMBER // every logged in user will be member
}

enum Gender {
    MALE
    FEMALE
    OTHERS
}

enum AddressType {
    HOME
    WORK
}

enum OauthMethod {
    EMAIL_PASS
    GOOGLE
    FACEBOOK
    APPLE
}

enum PromotionLevelStatus {
    Normal
    Hidden
}

enum SocialMediaStatus {
    Normal
    Hidden
}

enum NormalOrHiddenStatus {
    Normal
    Hidden
}

enum BlogLevel {
    Latest_Activity
    Recommend
    FAQ
}

enum OrderType {
    Product
    Parcel
}

enum TypeOfCharge {
    Percentage_of_products_amount
    Fixed_amount_for_each_pcs
}

enum CouponType {
    Full_Reduction
    Discount
}

enum CouponUse {
    All
    Order
    Waybill
}
